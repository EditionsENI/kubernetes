apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-mailhog
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: manual
  resources:
    requests:
      storage: 10Mi
  volumeName: pv-mailhog
