apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mailhog
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: manual
  capacity:
    storage: 10Mi
  hostPath:
    path: /tmp/pv-mailhog
