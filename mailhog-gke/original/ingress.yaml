apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mailhog
spec:
  rules:
  - host: "*.nip.io"
    http:
      paths:
      - path: /
        backend:
          serviceName: mailhog
          servicePort: 8025
