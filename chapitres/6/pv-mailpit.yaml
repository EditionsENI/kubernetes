apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mailpit
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  capacity:
    storage: 10Mi
  hostPath:
    path: /tmp/pv-mailpit
